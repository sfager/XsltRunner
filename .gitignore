# Ignore the .vscode folder which contains user-specific settings and should not be committed to version control for the original project
# This is a common practice to avoid sharing personal configurations and to prevent conflicts between different developers' settings.
# Remove ignore when starting a new project to allow sharing of settings like tasks if desired.
.vscode/

# results can be ignored since they are generated by the code and not needed for version control
results/    